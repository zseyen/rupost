# RuPost 环境变量配置模板
# 使用说明：将此文件重命名为 `.env` 并填写您的配置，RuPost 运行时会自动在同级目录加载。

# 场景二与场景三中真实大模型所需的 API Key
API_KEY=your_real_api_key_here

# (可选) 自定义大模型服务的基础 URL。若配置，可在模板中直接引用 {{env.BASE_URL}}
# BASE_URL=https://api.openai.com
