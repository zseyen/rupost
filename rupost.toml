# RuPost 配置文件示例
# 支持多环境配置和变量管理

# 开发环境
[environments.dev]
base_url = "https://httpbingo.org"
api_version = "v1"
# 使用系统环境变量
api_key = "${DEV_API_KEY}"
timeout = "30"
# 用户信息
test_user_email = "test@example.com"
test_user_password = "password123"

# 测试环境
[environments.test]
base_url = "https://httpbingo.org"
api_version = "v1"
api_key = "test-key-12345"
timeout = "60"
test_user_email = "tester@example.com"
test_user_password = "test123"

# Mock 环境（使用 httpbingo.org 的 anything 万能反射端点跑通所有业务 API 示例）
[environments.mock]
base_url = "https://httpbingo.org/anything"
api_version = "v1"
api_key = "mock-key-12345"
timeout = "30"
test_user_email = "test@example.com"
test_user_password = "password123"

# 预发布环境
[environments.staging]
base_url = "https://staging-api.example.com"
api_version = "v2"
api_key = "${STAGING_API_KEY}"
timeout = "60"
admin_token = "${STAGING_ADMIN_TOKEN}"

# 生产环境
[environments.prod]
base_url = "https://api.example.com"
api_version = "v2"
# 生产环境的 API Key 从环境变量读取，更安全
api_key = "${PROD_API_KEY}"
timeout = "120"
# 监控相关
monitoring_enabled = "true"
log_level = "info"
